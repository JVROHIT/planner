/**
 * This package is read-only and interpretive.
 * <p>
 * It must never mutate domain state.
 * It reads facts (DailyPlans, Snapshots) and derives meaning (Trends,
 * Statistics).
 * </p>
 */
package com.personal.planner.domain.analytics;
